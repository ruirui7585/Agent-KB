(function () {
  window.ProductPrototypeConfig = {
    defaultPage: "home",
    defaultScenario: "overview",
    toastDuration: 3000,
    enableAnnotationEditor: true,
    enableMockData: true,
    device: {
      width: 393,
      height: 852
    }
  };
})();

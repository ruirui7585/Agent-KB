# Annotation Rules

- Maintain one annotation entry per fixed pageId.
- Each entry must include page goal, core fields, click interactions, and business rules.
- Page changes in `prototype/app.js` must call the same navigation path so the annotation panel can sync by pageId.
- Do not delete the annotation panel or `annotation-data.js`.

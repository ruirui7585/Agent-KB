# 需求拷问

仅在用户选择需求拷问时读取。

围绕产品目标、核心用户、场景、范围、约束和验收标准逐项确认；每次只问一个会改变方向、范围、成本或验证的问题，并给出推荐答案。

能从 `PROJECT.md`、`SOURCES.md`、已有资料或环境中确定的事实先自行查证，只把需要用户决策的内容交给用户。

达成共同理解前不进入 PRD 拆分或执行；确认结果先登记到 `SOURCES.md`，通过采集门禁后再写入 `PROJECT.md` 和 `PLAN.md`。

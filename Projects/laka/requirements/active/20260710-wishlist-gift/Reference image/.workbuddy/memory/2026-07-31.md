# 2026-07-31 工作日志

## 1v1 心愿礼物 · 画布式静态交互原型（9 屏）

### 需求对齐
- 用户确认交付形式：**单页画布式**（一个 HTML 平铺全部页面，供设计评审）；范围：送礼方 P1-P6 + **接收方视角**。
- 输入：上级目录 HTML PRD（index.html/style.css/app.js，仅聊天页单页）+ 2 张参考截图。

### 产出
- `20260710-wishlist-gift/prototype/index.html`（574 行，零 JS 纯静态，375×812 移动端 ×9 屏，3 行 × 3 列画布）
- 屏清单：P1 聊天页心愿入口（1:1 延续 PRD）→ P2 心愿面板（置顶心愿进度+6 礼物宫格+余额）→ P3 赠送确认弹窗 → P4 余额不足充值（分支，橙色标记）→ P5 赠送成功全屏反馈 → P6 聊天页礼物消息卡片（入口进度同步 4/5）｜ P7 她的心愿列表（管理/统计/已达成）→ P8 添加心愿半屏（选礼物+数量步进）→ P9 心愿达成庆祝弹窗
- 数据一致性：心愿故事线 Crystal Heart 3/5 → 4/5 → 5/5 贯穿 P2-P9；礼物定价 Rose44/Lily99/Heart199/Crown520/Necklace999/Ring1314
- 视觉：延续 PRD 粉紫体系（#ff73dc→#c47cff 渐变、#a14cff 主色）；图例紫=送礼方、粉=接收方、橙=分支
- 资源引用：`../avatar_male.png`、`../Reference%20image/img_v3_...png`（已校验存在）

### 待确认问题（已向用户反馈）
- PRD 入口文案 "Her Wish" 与聊天对象 Amar（男性头像、"He sent"）性别指向不一致，原型按 PRD 原样保留；接收方页面对手命名为 Faisal 占位，待 PRD 明确。

## 入口形态优化（第二轮，按竞品参考图）
- 用户提供竞品截图（已存档 `Reference image/entry-reference.jpg`）：心愿入口为**贴右边悬浮浮标**——紫色渐变方块（64px 宽、左圆角 16px、右侧贴屏边）、心愿图标瓷贴、下方深色 pill 显示进度 0/10、左缘带收起缺口 ‹。
- 已将原型入口从「资料卡右上 Banner（Her Wish + 6 轮播点）」替换为该浮标形态：`.wfx` / `.wfx-ic` / `.wfx-pg` / `.wfx-notch`；P1（3/5，带粉色虚线标注环）、P2-P5 聊天背景（3/5）、P6（4/5 进度同步）共 6 屏已更新；删除旧 .wge/.wdots 代码。
- P2 半屏面板标题 "Her Wish List 💝" 保留不变。

## P2 心愿面板重构（第三轮，按竞品面板参考图）
- 用户提供面板参考图（已存档 `Reference image/panel-reference.jpg`），要求：送礼方只见 **3 个心愿**、每个显示**已送进度**、每个**单独赠送**、文案保持英文。
- P2 新结构：居中标题 "Amar's Gift Wishes 🌸" + ✕ → 灰色规则说明条（Complete a wish to earn +10 Intimacy bonus. Daily wishes reset at 0:00.）→ 紫色渐变大卡 "✦ Wish Progress ✦"：3 列心愿（编号徽章 1/2/3 + 图标 + 名称 + 进度 3/5、1/3、12/20 + 迷你进度条）→ 每列下方独立 Send 按钮（含钻价 💎199/1314/44）→ 底部余额行（💎320 + Recharge）。
- 心愿故事线更新：#1 Crystal Heart 3/5（主线，Send → P3）、#2 Diamond Ring 1/3、#3 Rose 12/20。
- 已删除旧 6 礼物宫格/置顶心愿卡相关 HTML 与 CSS（.feat/.ggrid/.gcell 等），新增 .desc/.wcard/.wcol/.wbadge/.wc-send 系列样式；.hot 独立样式移除（P4 的 .pack.hot 规则保留不受影响）。

## 删除 P3 二次确认弹窗（第四轮）
- 用户决策：**点 Send 直赠，不要二次确认弹窗**，P3 整屏删除。
- 画布调整为 3-2-3 布局（共 8 屏）：行1 = P1 → P2 → P4(分支)，行2 = P5 → P6，行3 = P7 → P8 → P9。P 编号不重排（保留 P4-P9 原编号便于沟通）。
- 同步更新：画板头图例（主线 P1→P2→P5→P6，注明"点 Send 直赠，无二次确认"；8 屏）、P2 note（点 Send 直接赠送 → P5）、P4 note（点 Send 后余额不足时拦截，位置移至行1第三格）。
- 屏号映射（现行）：P1 聊天入口 / P2 心愿面板 / P4 余额不足(分支) / P5 赠送成功 / P6 礼物消息 / 入口(接收方) / P7 她的心愿列表 / P8 心愿配置 / P9 心愿达成。

## 接收方入口屏 + P8 同页配置（第五轮）
- 接收方补充「入口」屏：聊天页右侧悬浮浮标，**粉色变体** `.wfx.her`（linear-gradient 粉色 #ff7ab8→#d93687，区别于送礼方紫色 `.wfx`），对象 Faisal、浮标文案 "My 3/3"、点击 → P7。与送礼方 P1 同构但样式/文案差异化。
- P8 重构为「选礼物+数量同页」：新增 CSS `.cgrid/.ccell(`.sel`/`.lock`)/.qtyrow/.step/.selcount/.cfg-save/.locktag`；礼物网格 6 选 3（Rose/Lily/Heart 选中打勾，Crown 可选未选，Necklace💎999 / Ring💎1314 超价格上限灰色锁定显示 "Over cap"），下方每个已选礼物配独立数量步进器 + 价格上限标签（Price cap 💎100/200/300），selcount 显示 "3/3 max reached"。删除旧 `.gchips/.gchip/.qty` 结构。
- P7 note 同步（由入口浮标进入）；画板头版本号 → v1.4；屏数回到 9（接收方链路 4 屏：入口→P7→P8→P9，3+2+4=9）。
- 价格上限规则已视觉化：避免主播选高价值礼物（Ring/Necklace 锁定不可选）。

## P1 入口浮标加礼物进度条（第六轮，微调）
- 用户要求 P1 入口浮标增加礼物进度条。新增 CSS `.wfx-bar`/`.wfx-bar i`（宽 44px、高 4px、浅色底+粉→暖渐变填充）；在 P1 浮标 `.wfx-pg` 下方加 `<div class="wfx-bar"><i style="width:60%"></i></div>`（3/5 = 60%）。
- 仅改 P1（编号为 P1 的聊天入口屏，用 ring 标注前缀精确定位）；P2-P5 聊天背景浮标被遮罩覆盖、接收方粉色入口未改动，保持一致待确认。

## P8 增加「特殊礼物」标记（第七轮）
- 用户要求 P8 礼物选择支持"特殊礼物"概念，可在某个礼物上做标记。
- 新增 CSS：`.cc-sp`（金色星标切换按钮，左上角，☆待标记/★已标记）、`.ccell.special`（金色边框+浅金底高亮）、`.spectag`（金色 "SPECIAL" 角标）。
- P8 落地：3 个已选礼物（Rose/Lily/Heart）各加 `cc-sp` 星标；其中 Heart 设为 `special` 态（★实心 + SPECIAL 标签 + 金色边框），对应送礼方 P2 面板里的置顶心愿 #1（概念自洽）。
- 数量行 Heart 副文案加 "⭐ special"；desc 加 "Tap ★ to mark one gift as your special top wish"；P8 note、legend、版本号 v1.4→v1.5 同步。
- 约定：特殊礼物至多 1 个（=接收方的置顶心愿），仅已选礼物可标记，锁定/未选礼物不加星标。

## P8 去掉「星星角标」（第八轮，微调）
- 用户要求去掉 P8 礼物卡上的 ★/☆ 星标切换角标（.cc-sp）。已删除 .cc-sp CSS 及 3 个礼物卡的 `<span class="cc-sp">`；desc 改为 "Mark one gift as your special top wish — shown with a gold highlight."
- 特殊礼物概念保留：已标记礼物用 `.ccell.special`（金色边框+浅金底） + `.spectag`（SPECIAL 金色文字标签）呈现，不再用星标控件。Heart 为默认已标记态。

## P7 FULFILLED 语义改为「完成心愿的送礼人数」+ 去编辑 banner（第九轮）
- 用户明确：P7 的 FULFILLED 应展示**完成该心愿的送礼人数**，不是"已达成心愿"。
- 顶部统计卡 `🎉 1 Fulfilled` → `🎉 8 Senders fulfilled`（完成心愿的送礼人数维度）。
- FULFILLED 区卡片 Royal Crown 文案 `3/3 · completed on Jun 20` → `Fulfilled by 8 senders · Jun 20`；保留 `FULFILLED 🎉` 徽章（表示心愿已达成）。
- 去除 FULFILLED 区卡片的编辑按钮 `<div class="wedit">✎</div>`；IN PROGRESS 区 3 个进行中心愿的 ✎ 保留（可编辑）。
- 版本号 v1.5 → v1.6。

## P7 去除 FULFILLED 整区 UI（第十轮，微调）
- 用户截图标注 P7 的 FULFILLED 区（含 `FULFILLED` 标题 + Royal Crown 已达成卡片）需整块去除。
- 已删除 `<div class="sec-t">FULFILLED</div>` 及其下 `<div class="wrow done">`（Royal Crown 卡片：图标+名称+FULFILLED徽章+进度条+Fulfilled by 8 senders）。
- 顶部统���卡 `🎉 8 Senders fulfilled` 保留不变；P7 现在只展示 IN PROGRESS 区 3 个进行中心愿 + 底部 Edit my wishes 入口。

## P2 去除礼物左上角数字徽章（第十一轮，微调）
- 用户要求 P2 心愿面板里 3 个礼物（Crystal Heart/Lily/Rose）左上角数字徽章（wbadge 1/2/3）去除。
- 删除 3 个 `<span class="wbadge">` 及 `.wbadge` CSS 死规则；礼物卡片其余（图标/名称/进度/数量/发送按钮）不变。

## P2 Send 按钮去除价格（第十二轮，微调）
- 用户要求 P2 每个心愿的 Send 按钮不展示价格。已把 3 个 `.wc-send` 内的 `<small>💎 199/99/44</small>` 价格小字移除，仅保留 "Send" 文案；并清理 `.wc-send small` 死 CSS 规则。

## P4 改为半屏充值面板（第十三轮）
- 参考竞品截图，P4 从居中「余额不足」确认弹窗改为**底部半屏 Recharge 面板**。
- 新 UI：居中标题 "Recharge" + ✕ 关闭 + 6 个充值包 3×2 网格（金币图标 + 钻数 + USD 价格），第 3 个标 HOT 高亮；底部 Terms / Privacy 条款链接。
- 新增 CSS：`.rch` / `.rch-grid` / `.rch-pk` / `.rch-pk hot` / `.rch-ic` / `.rch-foot` 系列。旧的 `.dlg`/`.packs`/`.pack` 不再被 P4 引用（P9 庆祝弹窗仍保留使用）。
